version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: starline-postgres
    environment:
      POSTGRES_USER: starline_admin
      POSTGRES_PASSWORD: starline2024
      POSTGRES_DB: starline_oa
      TZ: Asia/Shanghai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U starline_admin -d starline_oa"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
